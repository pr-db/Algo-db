#include <bits/stdc++.h>
using namespace std;
typedef long long int ll;
#define FastIO                        \
	ios_base::sync_with_stdio(false); \
	cin.tie(NULL);                    \
	cout.tie(NULL)

struct Edge{
	ll u, v, w;
};

ll fin

int main()
{

	int t;
	cin >> t;
	Edge a[t];
	for (ll i = 0; i < t;i++)
		cin >> a[i].u >> a[i].v >> a[i].w;




	return 0;
}